name: formatter-backend
project:
  type: python
  start: uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT
  build:
    - pip install --upgrade pip
    - pip install -r requirements.txt
env:
  # Defina na Square Cloud: URL do frontend (ex.: https://seu-frontend.square.app)
  FRONTEND_ORIGIN: https://seu-frontend.square.app



